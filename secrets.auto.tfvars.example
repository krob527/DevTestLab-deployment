# Copy to secrets.auto.tfvars (kept out of git) and fill in real passwords.
# This file is an example and can be committed; do NOT commit the filled secrets file.

passwords = {
  chris = "REPLACE_ME"
  fidel = "REPLACE_ME"
  kevin = "REPLACE_ME"
  miles = "REPLACE_ME"
}
